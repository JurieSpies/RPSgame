/**
 * @typedef RouteObject
 * @property {string} name - Name of the screen, used to navigate to it.
 * @property {() => JSX.Element} component - Component that should be rendered when this screen is activate.
 * @property {object} initialParams - Initial parameters that get passed into the screen component.
 */
