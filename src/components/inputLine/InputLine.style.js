import { StyleSheet } from 'react-native';
import { COLOR_GREY } from '../../common/constants/style';

const styles = StyleSheet.create({
  mainContainer: {
    marginHorizontal: 30,
  },
  line: {
    borderBottomWidth: 1,
    borderColor: COLOR_GREY,
  },
});
export default styles;
